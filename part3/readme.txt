To compile and execute the program:

from the source code
 -to run comment out the if statement in the main method and change args[0] to the file location
 - it should run after this

2- Execute the compiled files using the command:
    java -jar Perceptron.jar (file src)

 
